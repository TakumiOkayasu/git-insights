# Repository Graph — 第1段階の画面確認

final result: passed

対象は第1段階のCommit Graphとリモート一覧。GitLens全体の完全一致・機能同等性の判定ではない。

- Source visual truth: https://help.gitkraken.com/wp-content/uploads/commit-graph.png
- Source capture: 作業領域 `work/graph-reference.png`
- Implementation screenshot: 作業領域 `outputs/repository-graph.png`
- Implementation: 拡張と同じビルド済みJS/CSSを使う参照専用プレビュー。Git Insightsリポジトリの実データを取得。
- Viewport: 1280×720 CSS px。両キャプチャは1280×720 px。同じブラウザーのスクリーンショットを同じ比較入力に表示。
- State: 全ブランチのグラフ、ダークテーマ、コミット未選択。参照にはVS Code本体の枠が含まれ、実装キャプチャはWebview内容のみ。リポジトリ・コミット数・言語が違うため、ピクセル単位の一致判定はしない。

## 比較と修正

1. 初回の比較で、常時表示する空の詳細ペインがグラフを圧迫し、SHA列が見切れるP2を確認。未選択時は詳細を閉じ、幅1500px以下では選択後の詳細を表の下へ配置した。
2. 狭いグラフ列の日本語見出しが折り返されるP2を確認。単一行と省略表示に統一した。
3. ブランチ名が追跡数と衝突するP2を確認。名前の領域に省略表示を設け、ahead/behindは矢印と数値の固定領域に分けた。
4. 修正後に参照と1280×720の全体キャプチャを再比較。表の各列、ヘッダー、左側一覧の見切れが解消。900×720でも検索を折り返して全ツールバー操作が利用できることを確認。表は必要時に横スクロールする。

## 必須の確認面

- Typography: VS Codeのフォント変数、本文12px、28pxのコミット行。長い名前はツールチップと省略表示。参照のコンパクトな表形式を維持。
- Layout: 左のブランチ一覧、参照ラベル、グラフ、件名、著者、日付、SHAの順序。カード型の履歴表示から変更した。
- Color: VS Codeテーマ変数に追従。分岐線、local/remote/tagラベルに意味のある色を割り当てる。
- Assets: Microsoft Codiconsを使用。コミットグラフは親SHAから生成するデータ可視化であり装飾画像ではない。参照のユーザー画像は再使用せず、グラフ内アバターはこの段階では未実装。
- Copy: 日本語／英語。リモート参照はローカルの記録であること、検索が読み込み済みの範囲であることを表示。

全体比較の中でヘッダーとブランチ領域を読み取れる解像度だったため、別の拡大キャプチャは不要と判断した。

## 操作確認

- 実データのコミット選択から変更ファイル3件を表示。
- 検索で一致件数を更新し、一致しない行を薄く表示。
- mainとorigin/mainを共通祖先から比較し、変更ファイルを表示。
- リモートブランチだけに絞り込み、全ブランチ表示へ戻す。
- ブラウザーのwarning/errorログは0件。
- 別途VS Code起動テストで新しいグラフタブが開くことを確認。

プレビューは参照専用。Fetchはローカルのテスト用リモートで検証済み。公開サービスへのFetch、WSL実機操作、グラフからの差分クリックを含むVS Code UI全体の自動操作は未検証。

## 後続の改善

- P3: 表示列の幅調整、参照ラベルの折りたたみ、グラフ内アバターは後続段階。
- Worktrees・Stash・Launchpadなどの未実装機能は `docs/gitlens-parity.md` で追跡する。
